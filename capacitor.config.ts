
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.lovable.608a9ce8c47441f18e7e1e022a972f9d',
  appName: 'callify-glass',
  webDir: 'dist',
  // Remove the server configuration to make it a fully bundled app
  bundledWebRuntime: true
};

export default config;
